TAVILY_API_KEY=api-key-1,api-key-2
MCP_ALLOWED_ORIGINS=https://chatgpt.com

# Required for Worker unless MCP_ALLOW_PUBLIC=true.
# Generate: openssl rand -base64 32 | tr '+/' '-_' | tr -d '='
# MCP_PATH_TOKEN=

# Demo only — serves public POST /mcp without a path token
# MCP_ALLOW_PUBLIC=true

# Optional worker knobs (safe defaults live in wrangler.jsonc)
# MCP_ENABLED=true
# MCP_DAILY_REQUEST_LIMIT=10000
# MCP_REQUESTS_PER_MINUTE=120
# MCP_MAX_REQUEST_BODY_BYTES=1048576
# TAVILY_DAILY_CALL_LIMIT=1000
# RESEARCH_JOB_TTL_SECONDS=86400
# KEY_QUARANTINE_SECONDS=600
# MAX_RESEARCH_JOBS=2000
